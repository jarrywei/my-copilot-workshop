# 變更紀錄

## [0.2.0] - Step 2: 深色模式切換與篩選功能

### 新增
- 新增標題列右側的深色模式切換按鈕。
- 新增深色 / 淺色模式切換，按鈕顯示圖示與文字。
- 新增 `localStorage` 儲存使用者主題偏好，重新整理後可維持設定。
- 新增系統主題自動偵測功能：如果使用者尚未手動設定，會跟隨作業系統的深淺色模式。
- 新增三種篩選按鈕：全部、未完成、已完成。
- 新增篩選後的空清單提示文字，依不同篩選狀態顯示對應訊息。

### 變更
- 調整介面配色，透過 CSS 變數支援淺色與深色主題切換。
- 更新待辦列表渲染邏輯，讓篩選狀態與資料展示保持同步。
- 維持底部「未完成:N 項」計數為整體數量，不受篩選條件影響。
- 優化 UI 與 RWD 表現，確保在不同螢幕尺寸下仍可正常使用。

## [0.1.0] - Step 1: 基本的新增、完成、刪除、未完成計數、localStorage 保存

### 新增
- 新增輸入框與新增按鈕，可建立新的待辦事項。
- 新增待辦項目的勾選框，勾選後可標示完成狀態。
- 新增刪除按鈕，可移除單一待辦項目。
- 新增未完成數量顯示，底部會顯示目前未完成項目總數。
- 新增 `localStorage` 保存功能，讓待辦資料在重新整理後仍保留。
- 新增空清單提示文字，當沒有任何待辦事項時顯示提醒。

### 變更
- 優化待辦項目的視覺樣式，包括卡片式設計、圓角與陰影效果。
- 調整完成狀態的顯示效果，已完成項目會加上刪除線並降低透明度。
- 更新渲染邏輯，使新增、勾選與刪除操作能即時同步到畫面與儲存資料。
- 維持純 HTML、CSS、原生 JavaScript 的架構，不依賴任何第三方套件。
